# Projeto Python com MVC

# 💻 Sobre o Projeto
Este projeto é uma aplicação simples de gerenciamento de dados para uma biblioteca, desenvolvida utilizando o padrão de arquitetura Model-View-Controller (MVC) em Python.

O objetivo principal é demonstrar a separação de responsabilidades, dividindo a lógica da aplicação em três componentes principais:

Model (models/): Gerencia os dados da aplicação (classes, regras de negócio e acesso ao banco de dados, se houver).

View (views/): Responsável pela interface do usuário e pela apresentação visual dos dados.

Controller (controllers/): Atua como intermediário, recebendo a entrada do usuário (da View) e chamando os métodos apropriados no Model.

# 🚀 Funcionalidades Principais
Gerenciamento de Livros (livro_controller).

Gerenciamento de Autores (autor_controller).

# ⚙️ Tecnologias Utilizadas
Linguagem: Python

Arquitetura: MVC (Model-View-Controller)
